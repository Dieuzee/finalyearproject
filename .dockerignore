# Virtualenvs
venv/
.venv/

# Python caches
__pycache__/
*.pyc

# Git
.git/
.gitattributes
.gitignore
.dockerignore
Dockerfile
docker-compose.yml

# Model files - no longer used (diagnosis is done via an Ollama vision model)
*.h5

# Training data & scripts (not needed for serving)
Main_folder/
camera.py
inspect_model.py

# Runtime-generated uploads (any case)
static/shots/*

# Secrets & OS cruft - never copy the real key into the image
.env
.env.*
!.env.example
.DS_Store
